export default function TechnicalCards({ health }) {
    const cards = [["MODEL", "U-NET++", "ResNet-50 encoder"], ["INPUT", "RGB", "256 x 256"], ["TASK", "SEMANTIC SEGMENTATION", ""], ["BACKEND", "FASTAPI", "HTTP inference"], ["MASK SUPPORT", "READY", "Future API output"]];
    return <section className="technical-grid" aria-label="Technical overview"><article className="tech-card terminal"><span className="eyebrow">PIPELINE TRACE / STATIC</span><h3>REAL-TIME INFERENCE</h3><pre>{`$ corneaai /predict\n> image received\n> preprocessing\n> model inference\n> result returned`}<span className="cursor">_</span></pre></article>{cards.map(([label, value, description]) => <article className="tech-card" key={label}><span className="eyebrow">{label}</span><strong>{value}</strong><p>{description}</p></article>)}<article className="tech-card"><span className="eyebrow">SYSTEM STATUS / LIVE</span><strong>{health?.status === "ok" ? "API CONNECTED" : "API OFFLINE"}</strong><p>Device: {health?.device || "-"}</p></article></section>;
}
